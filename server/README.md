# Financer - Backend

This backend provides APIs for the Financer app.

Env variables (see .env.example):
- PORT
- MONGO_URI
- JWT_SECRET
- SMTP_HOST
- SMTP_PORT
- SMTP_USER
- SMTP_PASS
- EMAIL_FROM

Run:

npm install
npm run dev
